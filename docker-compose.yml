services:
  frontend:
    build:
      context: ./mega_money
      dockerfile: Dockerfile
    container_name: mega_money_frontend
    expose:
      - "3000"
    networks:
      - megamoney_network

networks:
  megamoney_network:
    external: true